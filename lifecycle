Maven lifeCycle description:

clean: Removes files, such as .class files and .jar files, generated by previous builds.

javadoc: The maven-javadoc-plugin helps us generate project documentation (such as Javadoc) during the lifecycle

test reports: The "site" phase includes test reports. Test reports are often generated with plugins like
surefire-report-plugin. These reports gives us the result of unit or integration tests.

test: Runs tests using frameworks like JUnit.

package: Packages the compiled code into format such as .jar or .war.
